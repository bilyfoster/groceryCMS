version: '3.9'

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - brazen-internal
    restart: unless-stopped

  backend:
    build: ./backend
    env_file: .env
    environment:
      SPRING_PROFILES_ACTIVE: prod
      POSTGRES_HOST: db
    volumes:
      - media_data:/app/media
    depends_on:
      - db
    networks:
      - brazen-internal
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.brazen-api.rule=Host(`brazen.1lpro.com`) && (PathPrefix(`/api`) || PathPrefix(`/media`))"
      - "traefik.http.routers.brazen-api.entrypoints=websecure"
      - "traefik.http.routers.brazen-api.tls=true"
      - "traefik.http.routers.brazen-api.tls.certresolver=letsencrypt"
      - "traefik.http.services.brazen-api.loadbalancer.server.port=8080"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_APP_VERSION: ${APP_VERSION:-0.1.0}
    env_file: .env
    environment:
      BACKEND_URL: http://backend:8080
    depends_on:
      - backend
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.brazen-app.rule=Host(`brazen.1lpro.com`)"
      - "traefik.http.routers.brazen-app.entrypoints=websecure"
      - "traefik.http.routers.brazen-app.tls=true"
      - "traefik.http.routers.brazen-app.tls.certresolver=letsencrypt"
      - "traefik.http.routers.brazen-app.priority=5"
      - "traefik.http.services.brazen-app.loadbalancer.server.port=3000"
    restart: unless-stopped

volumes:
  postgres_data:
  media_data:

networks:
  brazen-internal:
    driver: bridge
  traefik:
    external: true
